---
platform: windows

inputs:
- name: ci
- name: repo

outputs:
- name: output-binary

run:
  path: powershell
  args:
  - "-ExecutionPolicy"
  - "Bypass"
  - "-File"
  - ci/build-binary/run.ps1

params:
  GOPATH: repo
  PACKAGE:
  BINARY:
