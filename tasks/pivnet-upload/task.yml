---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: concourse/git-resource

inputs:
  - name: ci
  - name: stemcell
  - name: pivnet-release
outputs:
  - name: pivnet-metadata
run:
  path: ci/tasks/pivnet-upload/run2
params:
  OS_NAME:

