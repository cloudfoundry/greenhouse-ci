---
platform: linux

image_resource: {type: docker-image, source: {repository: pivotalgreenhouse/ci}}

inputs:
- name: ci
- name: repo

run:
  path: ci/tasks/run-ginkgo/run

params:
  GOPATH: "repo"
  TEST_PATH: "repo"
  IGNORE_PACKAGES:
