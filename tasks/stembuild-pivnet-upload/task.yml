---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: concourse/git-resource

inputs:
  - name: ci
  - name: stembuild
  - name: pivnet-release
run:
  path: ci/tasks/stembuild-pivnet-upload/run
params:
  OS_NAME:
  PIVNET_API_TOKEN:
  PIVNET_ENDPOINT:
  PRODUCT_SLUG:

