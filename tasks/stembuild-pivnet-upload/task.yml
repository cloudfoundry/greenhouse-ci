---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: pivotalgreenhouse/pivnet

inputs:
  - name: ci
  - name: pivnet-release
run:
  path: ci/tasks/stembuild-pivnet-upload/run
params:
  OS_NAME:
  PIVNET_API_TOKEN:
  PIVNET_ENDPOINT:
  PRODUCT_SLUG:

