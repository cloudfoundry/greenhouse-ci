---
platform: linux

image_resource: {type: docker-image, source: {repository: pivotalgreenhouse/ci}}

inputs:
  - name: ci
  - name: repo

outputs:
  - name: updated_repo

run:
  path: ci/scripts/update-tar.sh

params:
  PATH:
