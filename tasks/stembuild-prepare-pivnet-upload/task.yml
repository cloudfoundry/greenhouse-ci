---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: concourse/git-resource

inputs:
  - name: ci
  - name: stembuild
  - name: pivnet-release
outputs:
  - name: pivnet-metadata
run:
  path: ci/tasks/stembuild-prepare-pivnet-upload/run
params:
  OS_NAME:

