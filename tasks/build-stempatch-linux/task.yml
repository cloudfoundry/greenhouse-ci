---
platform: linux
image_resource: {type: docker-image, source: {repository: pivotalgreenhouse/ci}}

inputs:
  - name: librsync
  - name: stembuild
  - name: ci
  - name: stempatch-version
  - name: ovftool

outputs:
  - name: output

run:
  path: /bin/bash
  args:
    - -c
    - ci/tasks/build-stempatch-linux/run.sh
