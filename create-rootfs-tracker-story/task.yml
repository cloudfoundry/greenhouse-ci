
---
image_resource:
  type: docker-image
  source:
    repository: pivotalgreenhouse/ci

platform: linux

inputs:
- name: ci
- name: version

params:
  TRACKER_TOKEN:
  TRACKER_PROJECT_ID:

run:
  path: ci/create-rootfs-tracker-story/run
