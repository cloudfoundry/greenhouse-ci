---
platform: linux

image_resource: {type: docker-image, source: {repository: pivotalgreenhouse/ci}}

inputs:
- name: ci
- name: buildpack

run:
  path: ci/update-buildpack/run

params:
  CF_ADMIN_PASSWORD:
  CF_ADMIN_USER:
  CF_API:
  BUILDPACK_NAME:
