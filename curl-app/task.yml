---
image_resource:
  type: docker-image
  source:
    repository: pivotalgreenhouse/ci

platform: linux

inputs:
- name: ci

params:
  CF_APP_URL:
  CF_TAG:
  DATADOG_API_KEY:
  DATADOG_APPLICATION_KEY:

run:
  path: ci/curl-app/run
