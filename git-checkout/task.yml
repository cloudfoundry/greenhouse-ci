---
platform: linux

image_resource: {type: docker-image, source: {repository: pivotalgreenhouse/ci}}

inputs:
- name: ci
- name: repo

run:
  path: ci/git-checkout/run

outputs:
- name: cleaned-repo
